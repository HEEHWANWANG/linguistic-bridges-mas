# Audio Processing & Analysis
librosa>=0.10.0
numpy>=1.24.0
scipy>=1.10.0

# LLM Clients
anthropic>=0.7.0  # Claude API (recommended, no OpenAI subscription needed!)
requests>=2.31.0  # For Ollama and general HTTP
ollama>=0.0.21    # Optional: For local model support

# Image Generation (Hugging Face)
torch>=2.0.0      # PyTorch (install separately if needed: pip install torch)
diffusers>=0.21.0  # Stable Diffusion models
transformers>=4.30.0  # Hugging Face transformers
safetensors>=0.3.1  # Safe model loading
pillow>=9.0.0     # Image processing

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0

# Development (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
